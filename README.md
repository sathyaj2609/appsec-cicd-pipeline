# appsec-cicd-pipeline
Automated AppSec CI/CD pipeline — SAST (Semgrep + custom OWASP-mapped rules), SBOM &amp; dependency scanning (Syft/Grype), with a Python policy gate that fails builds on critical findings.
